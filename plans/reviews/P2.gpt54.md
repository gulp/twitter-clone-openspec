Reviewed against the uploaded plan

```diff
--- a/twitter-clone.md
+++ b/twitter-clone.md
@@
 ### 1.9 Feed Assembly Strategy
-The design doc confirms fan-out-on-read. Home timeline query uses a single UNION query to avoid race conditions at page boundaries:
+**Issue:** The current feed dedup query keeps the *oldest* `effectiveAt` per tweet because `DISTINCT ON` is paired with `ORDER BY "tweetId", "effectiveAt" ASC`. That mis-ranks tweets that were recently retweeted by a followed user and makes page-boundary behavior unstable.
+
+**Reasoning:** For a fan-out-on-read home timeline, dedup must keep the newest signal for each tweet (original publish or most recent retweet by a followed account). Hydration must also preserve the SQL ordering; otherwise the final response order can drift from the cursor order.
+
+**Improvement:** Keep the newest `effectiveAt` per `tweetId`, add a deterministic tie-breaker, and carry the ordering through hydration.
+
+The design doc confirms fan-out-on-read. Home timeline query uses a single UNION query to avoid race conditions at page boundaries:
@@
 deduped AS (
   SELECT DISTINCT ON ("tweetId") *
   FROM feed_items
-  ORDER BY "tweetId", "effectiveAt" ASC
+  ORDER BY "tweetId", "effectiveAt" DESC, "retweeterId" DESC NULLS LAST
 )
 SELECT * FROM deduped
-ORDER BY "effectiveAt" DESC
+ORDER BY "effectiveAt" DESC, "tweetId" DESC
 LIMIT 21;
```

Then:
-1. Hydrate tweet + author data for the resulting IDs (single batched query)
-2. Batch-check hasLiked/hasRetweeted for all tweet IDs (see §1.16)
+1. Hydrate tweet + author data for the resulting IDs in a way that preserves the `deduped` order (`WITH ids AS (...) ... ORDER BY ids.ord` or an equivalent deterministic `CASE` expression)
+2. Batch-check `hasLiked`/`hasRetweeted` for all tweet IDs (see §1.16) 3. Cache assembled page in Redis using versioned key `feed:{userId}:v:{version}:page:{cursorHash}` with TTL 60s

-**Cache versioning:** Instead of a TTL-based invalidation flag, maintain a monotonic `feed:version:{userId}` counter in Redis. Bump on new tweet, follow/unfollow, retweet, or soft-delete. Cache reads check the version; a mismatch means the cache is stale and triggers a DB query. +**Home-feed cursor:** Because home feed ordering is by `effectiveAt DESC, tweetId DESC`, the opaque cursor payload for `feed.home` is `{ effectiveAt, tweetId }`, not the raw tweet `createdAt`. This avoids duplicates/skips when a retweet changes the effective ordering.

- +**Cache versioning:** Instead of a TTL-based invalidation flag, maintain a monotonic `feed:version:{userId}` counter in Redis. Bump on follow/unfollow, retweet, or soft-delete. On new tweet creation, also publish an SSE `new-tweet` event so active clients can refresh immediately even before cache expiry.
  @@

### 1.11 Full-Text Search Setup

-PostgreSQL `tsvector` column on Tweet with a GIN index. Use a generated column `searchVector tsvector GENERATED ALWAYS AS (to_tsvector('english', content)) STORED`. This avoids triggers and keeps the schema self-contained. The search query uses `plainto_tsquery` with `ts_rank` for ordering. +**Issue:** The plan uses both `searchVector` and `search_vector` to describe the generated column. That naming mismatch is easy for coding agents to implement incorrectly.

- +**Reasoning:** Prisma will not manage this generated `tsvector` field directly, so the raw SQL migration name must be canonical everywhere the plan references it.
- +**Improvement:** Standardize on PostgreSQL snake_case: `search_vector`. Use a generated column and GIN index, and keep all search SQL aligned to that exact column name.
- +PostgreSQL `tsvector` column on Tweet with a GIN index. Use a generated column `search_vector tsvector GENERATED ALWAYS AS (to_tsvector('english', coalesce(content, ''))) STORED`. This avoids triggers and keeps the schema self-contained. The search query uses `plainto_tsquery` with `ts_rank` for ordering.
  @@
  -### 1.13 User Select Pattern (hashedPassword exclusion)

  ***

  -Define a reusable Prisma `select` object in `src/server/db.ts`:
  +### 1.13 User Select Pattern (public vs self) +**Issue:** The current `publicUserSelect` includes `email`, which would leak private email addresses through public profile, follower, liker, and search endpoints.

- +**Reasoning:** Excluding `hashedPassword` is necessary but not sufficient; privacy-sensitive columns such as `email` and `sessionVersion` must also never be part of the default public shape.
- +**Improvement:** Define separate `publicUserSelect` and `selfUserSelect` objects in `src/server/db.ts`, and require all routers to choose the least-privileged shape.
-

```typescript
export const publicUserSelect = {
-  id: true, email: true, username: true, displayName: true,
+  id: true, username: true, displayName: true,
  bio: true, avatarUrl: true, bannerUrl: true, createdAt: true,
  followerCount: true, followingCount: true, tweetCount: true,
} as const;
+
+export const selfUserSelect = {
+  ...publicUserSelect,
+  email: true,
+} as const;
```

-Every query that returns user data MUST use `select: publicUserSelect` or a superset of it. NEVER use `include: { author: true }` without an explicit `select` — Prisma returns all columns by default, including `hashedPassword` and `sessionVersion`.
+Every query that returns user data MUST use `select: publicUserSelect` or `select: selfUserSelect` explicitly. Public endpoints (`getByUsername`, followers/following, likers, search, notifications actor payloads, tweet author payloads) MUST NOT return `email`. NEVER use `include: { author: true }` without an explicit `select` — Prisma returns all columns by default, including `hashedPassword` and `sessionVersion`.
@@

### 1.17 Security Headers

- -Configure in `next.config.ts` via the `headers()` function: +**Issue:** A static CSP with `script-src 'unsafe-inline'` materially weakens XSS protection, and `headers()` alone cannot produce per-request nonces.

* +**Reasoning:** This app is cookie-authenticated and renders user-generated content. Strong CSP, CSRF-resistant mutation handling, and transport hardening should be first-class requirements, not optional polish.
* +**Improvement:** Keep static security headers in `next.config.ts`, but generate the CSP per request in `src/middleware.ts` using a nonce. In production, also emit HSTS.
* +Static headers in `next.config.ts`:

- `X-Content-Type-Options: nosniff`
- `X-Frame-Options: DENY`
- `Referrer-Policy: strict-origin-when-cross-origin`
  +- `Permissions-Policy: camera=(), microphone=(), geolocation=()`
- `X-XSS-Protection: 0` (rely on CSP instead)
  -- `Content-Security-Policy: default-src 'self'; img-src 'self' https://*.amazonaws.com https://*.minio.*; script-src 'self' 'unsafe-inline'` (adjust for dev)
  +- `Strict-Transport-Security: max-age=31536000; includeSubDomains; preload` (production only)

* +Dynamic CSP in `src/middleware.ts`:
  +- `default-src 'self'`
  +- `base-uri 'self'`
  +- `frame-ancestors 'none'`
  +- `object-src 'none'`
  +- `form-action 'self'`
  +- `img-src 'self' data: blob: https://*.amazonaws.com https://*.minio.*`
  +- `style-src 'self' 'unsafe-inline'`
  +- `script-src 'self' 'nonce-{requestNonce}'`
  +- `connect-src 'self' https: http:`
  +- add `upgrade-insecure-requests` in production

### 1.18 Logging

@@
-Use structured JSON output in production via a thin wrapper:
+Use structured JSON output in production via a thin wrapper:

```typescript
// src/lib/logger.ts
+type LogFields = {
+  requestId?: string;
+  route?: string;
+  userId?: string;
+  errorCode?: string;
+  latencyMs?: number;
+} & Record<string, unknown>;
+
+const redact = (data?: LogFields): LogFields | undefined => {
+  if (!data) return data;
+  const clone = { ...data };
+  delete clone.password;
+  delete clone.hashedPassword;
+  delete clone.token;
+  delete clone.access_token;
+  delete clone.refresh_token;
+  return clone;
+};
+
export const log = {
-  info: (msg: string, data?: Record<string, unknown>) =>
-    console.log(JSON.stringify({ level: 'info', msg, ...data, ts: new Date().toISOString() })),
-  warn: (msg: string, data?: Record<string, unknown>) =>
-    console.warn(JSON.stringify({ level: 'warn', msg, ...data, ts: new Date().toISOString() })),
-  error: (msg: string, data?: Record<string, unknown>) =>
-    console.error(JSON.stringify({ level: 'error', msg, ...data, ts: new Date().toISOString() })),
+  info: (msg: string, data?: LogFields) =>
+    console.log(JSON.stringify({ level: 'info', msg, ...redact(data), ts: new Date().toISOString() })),
+  warn: (msg: string, data?: LogFields) =>
+    console.warn(JSON.stringify({ level: 'warn', msg, ...redact(data), ts: new Date().toISOString() })),
+  error: (msg: string, data?: LogFields) =>
+    console.error(JSON.stringify({ level: 'error', msg, ...redact(data), ts: new Date().toISOString() })),
};
```

-Use at key boundaries: tRPC error handler, auth failures, rate limit hits, Redis failures, SSE connection lifecycle. Do NOT log request bodies or sensitive data.
+Use at key boundaries: tRPC error handler, auth failures, rate limit hits, Redis failures, SSE connection lifecycle, health/readiness checks, and S3 upload verification failures. Every request gets a `requestId`; propagate it through tRPC context, SSE logs, and background scripts. Do NOT log request bodies or sensitive data.

- +### 1.19 Cookie-Authenticated Mutation Protection +**Issue:** The app uses cookie-based auth for protected mutations, but the plan does not require any explicit CSRF/origin validation for tRPC POST requests.
- +**Reasoning:** `SameSite=Lax` helps, but it is not an application-layer authorization control. A clear origin-check rule prevents cross-site mutation abuse and removes ambiguity for coding agents.
- +**Improvement:** Reject all unsafe methods (`POST`, `PUT`, `PATCH`, `DELETE`) on `/api/trpc`, `/api/auth`, and `/api/sse` unless:
  +1. `Origin` is present and matches the configured app origin or an allowed preview origin list,
  +2. `Host` / `X-Forwarded-Host` matches the same allow-list, and
  +3. the request carries the expected session cookie.
- +Implement this check centrally in `src/middleware.ts` and surface failures as `403 FORBIDDEN` with a structured warning log including `requestId`, `origin`, and `route`.
- +### 1.20 Media Upload Intent + Ownership Verification +**Issue:** Validating only the final URL origin/path is not enough to prove that an uploaded object belongs to the current user, actually exists, matches the claimed MIME type/size, or was freshly uploaded for this request.
- +**Reasoning:** Without a server-side upload manifest, clients can attach stale objects, race incomplete uploads, and accumulate orphaned objects. This is both a correctness and abuse-prevention gap.
- +**Improvement:** Introduce a `MediaUpload` table and a two-step upload flow:
  +1. `media.getUploadUrl` creates a `MediaUpload` row with owner, purpose, object key, expected content type, expected byte size, and `expiresAt`
  +2. Client uploads directly to S3/MinIO using the presigned URL
  +3. `media.confirmUpload` performs `HEAD` verification against S3/MinIO, validates owner/purpose/content-type/size/checksum, then marks the row `UPLOADED`
  +4. `tweet.create` and `user.updateProfile` accept `mediaUploadIds`, not raw URLs; server resolves owned, confirmed uploads to `mediaUrls` and marks them `ATTACHED` in the same transaction
- +Add a periodic cleanup script to mark expired pending uploads and delete orphaned objects older than the grace window.
- +### 1.21 Health Endpoint Semantics +**Issue:** The current single `/api/health` endpoint returns `503` when Redis or S3 is unavailable, even though the plan intentionally degrades gracefully for several Redis/S3 features.
- +**Reasoning:** Using one endpoint for both liveness and readiness can cause unnecessary orchestrator restarts and hide the difference between “process is healthy” and “optional dependency is degraded”.
- +**Improvement:** Split health checks:
  +- `GET /api/health/live` → returns `200` if the app process can answer requests
  +- `GET /api/health/ready` → returns `200` only when the app is ready for full service; Redis/S3 failures return a degraded payload and `503` only if the deployment requires them for the configured mode
- +Both endpoints must return structured JSON with `{ status, db, redis, s3, mode }`.
- +### 1.22 Search Pagination Contract +**Issue:** Section §1.2 says every paginated endpoint uses opaque cursor pagination, but the search plan still describes `LIMIT 21` queries with no cursor contract.
- +**Reasoning:** Search ordering needs stable tie-breakers or coding agents will implement inconsistent pagination, especially for equal-rank FTS results and equal follower counts in user search.
- +**Improvement:** Both `search.tweets` and `search.users` accept `{ query, cursor?, limit? }` and return `{ items, nextCursor }`.
  +- Tweet search order: `rank DESC, createdAt DESC, id DESC`; cursor payload `{ rank, ts, id }`
  +- User search order: `followerCount DESC, id DESC`; cursor payload `{ followerCount, id }`
  +- Reject empty/whitespace-only queries with `BAD_REQUEST`
  +- Enforce a minimum normalized query length of 2 characters to avoid pathological scans
- +### 1.23 Database Guardrails +**Issue:** Several invariants currently live only in application code: counts should never be negative, deleted tweets should always have `deletedAt`, and a tweet should never be empty when no media is attached.
- +**Reasoning:** Database-level guardrails catch bugs during concurrency, retries, and future maintenance, including mistakes made by coding agents or manual scripts.
- +**Improvement:** Add raw SQL `CHECK` constraints in the migration:
  +- `followerCount >= 0`, `followingCount >= 0`, `tweetCount >= 0`
  +- `likeCount >= 0`, `retweetCount >= 0`, `replyCount >= 0`
  +- `deleted = false AND deletedAt IS NULL` OR `deleted = true AND deletedAt IS NOT NULL`
  +- `char_length(content) > 0 OR cardinality("mediaUrls") > 0`
  @@

## 2. Directory Structure

```diff
twitter-clone/
+├── middleware.ts                 # CSP nonce + origin checks for unsafe methods
@@
├── scripts/
-│   └── reconcile-counts.ts       # operational: recompute denormalized counts
+│   ├── reconcile-counts.ts       # operational: recompute denormalized counts
+│   └── reap-expired-media.ts     # operational: delete expired/unattached uploads
@@
│   │       ├── trpc/[trpc]/route.ts  # tRPC handler
│   │       ├── auth/[...nextauth]/route.ts  # NextAuth
│   │       ├── sse/route.ts       # SSE endpoint
-│   │       └── health/route.ts    # Health check endpoint
+│   │       └── health/
+│   │           ├── live/route.ts  # Liveness probe
+│   │           └── ready/route.ts # Readiness/dependency probe
@@
-│   │   ├── db.ts                 # Prisma client singleton + publicUserSelect
+│   │   ├── db.ts                 # Prisma client singleton + publicUserSelect/selfUserSelect
@@
-│   │   │       └── media.ts      # pre-signed URL generation (all uploads)
+│   │   │       └── media.ts      # upload intent creation + confirmation
@@
│   │       ├── mention.ts        # @mention parsing
│   │       ├── feed.ts           # feed assembly + dedup logic
│   │       ├── email.ts          # email sending abstraction
+│   │       ├── media.ts          # upload intent verification + orphan cleanup helpers
│   │       ├── sse-publisher.ts  # Redis Pub/Sub event bus (in-memory fallback for tests)
│   │       └── rate-limiter.ts   # Redis rate limiter
```

@@
model User {
@@
accounts Account[]
passwordResetTokens PasswordResetToken[]

- mediaUploads MediaUpload[]
  }
  @@
  model Tweet {
  @@
  @@index([authorId, deleted, createdAt(sort: Desc), id(sort: Desc)])
  @@index([parentId])
  }
- +model MediaUpload {

- id String @id @default(cuid())
- ownerId String
- purpose MediaPurpose
- objectKey String @unique
- publicUrl String
- contentType String
- byteSize Int
- checksum String?
- status MediaUploadStatus @default(PENDING)
- createdAt DateTime @default(now())
- expiresAt DateTime
- uploadedAt DateTime?
- attachedAt DateTime?
-
- owner User @relation(fields: [ownerId], references: [id], onDelete: Cascade)
-
- @@index([ownerId, purpose, status])
- @@index([expiresAt])
  +}
- +enum MediaPurpose {

- TWEET
- AVATAR
- BANNER
  +}
- +enum MediaUploadStatus {

- PENDING
- UPLOADED
- ATTACHED
- EXPIRED
  +}
  @@
  **Post-migration raw SQL** (run via `prisma migrate` custom SQL):

```sql
@@
CREATE INDEX IF NOT EXISTS "Notification_unread_created_idx"
  ON "Notification" ("recipientId", "createdAt" DESC) WHERE "read" = false;
+
+-- Guardrail constraints
+ALTER TABLE "User"
+  ADD CONSTRAINT "User_counts_nonnegative_chk"
+  CHECK ("followerCount" >= 0 AND "followingCount" >= 0 AND "tweetCount" >= 0);
+
+ALTER TABLE "Tweet"
+  ADD CONSTRAINT "Tweet_counts_nonnegative_chk"
+  CHECK ("likeCount" >= 0 AND "retweetCount" >= 0 AND "replyCount" >= 0);
+
+ALTER TABLE "Tweet"
+  ADD CONSTRAINT "Tweet_deleted_consistency_chk"
+  CHECK (
+    ("deleted" = false AND "deletedAt" IS NULL) OR
+    ("deleted" = true AND "deletedAt" IS NOT NULL)
+  );
+
+ALTER TABLE "Tweet"
+  ADD CONSTRAINT "Tweet_content_or_media_chk"
+  CHECK (char_length(content) > 0 OR cardinality("mediaUrls") > 0);
```

@@

### Redis Failure Strategy

All Redis operations MUST be wrapped in try/catch. The failure policy varies by feature:

- +Replay buffers are capped with `LTRIM` after each push (`max 200` events per user) and renewed with `EXPIRE 300` so a reconnect storm cannot grow unbounded memory usage.
  @@
  -| `sse:replay:{userId}` | List | 5 min | Short replay buffer for `Last-Event-ID` recovery |
  +| `sse:replay:{userId}` | List | 5 min | Short replay buffer for `Last-Event-ID` recovery (`LPUSH` + `LTRIM 0 199`) |
  +| `sse:seq:{userId}` | String (integer) | none | Monotonic per-user SSE event sequence for `id:` fields |
  @@
  └── media

* └── getUploadUrl (mutation, protected) // all uploads: tweet, avatar, banner

- ├── getUploadUrl (mutation, protected) // create upload intent + presigned URL
- └── confirmUpload (mutation, protected) // HEAD-verify object and mark intent UPLOADED
  @@

### Phase A: Foundation (Tasks 1.1-1.7, 2.1-2.10)

@@
-| `next.config.ts` | Image domains (S3/MinIO), security headers (see §1.17) |
+| `middleware.ts` | Request nonce generation + origin enforcement for unsafe methods (see §1.17, §1.19) |
+| `next.config.ts` | Image domains (S3/MinIO), static security headers (see §1.17) |
@@
-| `src/server/db.ts` | Prisma client singleton with global dev cache + `publicUserSelect` (see §1.13) |
+| `src/server/db.ts` | Prisma client singleton with global dev cache + `publicUserSelect` / `selfUserSelect` (see §1.13) |
@@
-| `src/app/api/health/route.ts` | `GET /api/health` — checks DB, Redis, S3 connectivity. Returns `{ status, db, redis, s3 }` with 200 or 503. |
+| `src/app/api/health/live/route.ts` | `GET /api/health/live` — process liveness only |
+| `src/app/api/health/ready/route.ts` | `GET /api/health/ready` — DB/Redis/S3 readiness with degraded mode awareness |
@@
-| `src/lib/logger.ts` | Structured JSON logger (see §1.18) |
+| `src/lib/logger.ts` | Structured JSON logger with redaction + requestId support (see §1.18) |
+| `scripts/reap-expired-media.ts` | Cleanup pending/expired media upload intents and orphaned objects |
@@
EMAIL_FROM="[noreply@twitterclone.local](mailto:noreply@twitterclone.local)"

- +# Security
  +APP_ORIGIN="[http://localhost:3000](http://localhost:3000)"
  +ALLOWED_PREVIEW_ORIGINS=""

````
@@
-- `GET /api/health` returns `{ status: "ok", db: true, redis: true, s3: true }`
+- `GET /api/health/live` returns `200` while the app process is healthy
+- `GET /api/health/ready` returns structured dependency state and only returns `503` when the deployment mode is not ready to serve traffic
+- `publicUserSelect` excludes `email`, while `selfUserSelect` includes it only for self-scoped reads
@@
#### C1: Tweet Backend
@@
-- `create`: validate content length (§1.15), require text or media, parse mentions, create tweet in transaction (increment author's tweetCount), fire mention notifications. If `parentId` is provided, verify parent exists AND `deleted = false`; if deleted, throw `BAD_REQUEST` with "Cannot reply to a deleted tweet". If parent exists, increment parent's `replyCount` in the same transaction.
+- `create`: validate content length (§1.15), require text or media, parse mentions, and resolve only owned confirmed `mediaUploadIds` for tweet attachments. Create the tweet in a transaction (increment author's `tweetCount`, mark uploads `ATTACHED`), then fire mention notifications. If `parentId` is provided, verify parent exists AND `deleted = false`; if deleted, throw `BAD_REQUEST` with "Cannot reply to a deleted tweet". If parent exists, increment parent's `replyCount` in the same transaction.
@@
#### C2: Media Backend
@@
-| `src/server/trpc/routers/media.ts` | getUploadUrl mutation (all upload types) |
+| `src/server/trpc/routers/media.ts` | `getUploadUrl` + `confirmUpload` mutations |
+| `src/server/services/media.ts` | S3 `HEAD` verification, upload intent resolution, orphan cleanup helpers |
@@
-- Input: `{ filename: string, contentType: string, purpose: 'tweet' | 'avatar' | 'banner' }`
+- `getUploadUrl` input: `{ filename: string, contentType: string, byteSize: number, checksum?: string, purpose: 'tweet' | 'avatar' | 'banner' }`
- Validate contentType against allowed MIME types (image/jpeg, image/png, image/gif, image/webp)
-- Validate file size: max 5MB per image
+- Validate file size server-side before signing: max 5MB per image; max 4 tweet images
- Generate S3 key: `{purpose}/{userId}/{cuid()}.{ext}`
-- Return `{ uploadUrl: presignedPutUrl, publicUrl: finalUrl }`
+- Persist a `MediaUpload` row with owner, purpose, objectKey, `publicUrl`, expected content type, expected byte size, optional checksum, and `expiresAt`
+- Return `{ uploadUrl: presignedPutUrl, mediaUploadId, publicUrl }`
- Pre-signed URL expires in 10 minutes
-- **URL validation on tweet create:** verify mediaUrls match the expected S3 bucket origin (e.g., `https://{BUCKET}.s3.{REGION}.amazonaws.com/` or `http://localhost:9000/{BUCKET}/`) and path prefix matches `tweet/{userId}/`. Reject any URL pointing to external domains.
+- `confirmUpload({ mediaUploadId })`: perform S3/MinIO `HEAD`, validate object exists and matches the recorded metadata, then mark the row `UPLOADED`
+- `tweet.create` and `user.updateProfile` accept `mediaUploadIds`, not raw external URLs. Resolve only uploads where `ownerId = session.userId`, `status = UPLOADED`, `expiresAt > now()`, and `purpose` matches the mutation.
+- After successful attach, mark the upload rows `ATTACHED` in the same transaction. A cleanup script expires old `PENDING` rows and deletes orphaned objects.
@@
#### D2: Search Backend
@@
-**Key details:**
-- Tweet search: `SELECT *, ts_rank(search_vector, query) AS rank FROM "Tweet", plainto_tsquery('english', $1) query WHERE search_vector @@ query AND deleted = false ORDER BY rank DESC LIMIT 21`
+**Key details:**
+- Both endpoints use the standard pagination shape from §1.2: `{ query, cursor?, limit? } -> { items, nextCursor }`
+- Reject empty/whitespace-only queries with `BAD_REQUEST`
+- Enforce minimum normalized query length of 2
+- Tweet search (parameterized):
+  ```sql
+  SELECT t.*, ts_rank(t.search_vector, query) AS rank
+  FROM "Tweet" t, plainto_tsquery('english', $1) query
+  WHERE t.search_vector @@ query
+    AND t.deleted = false
+    AND (
+      $2::numeric IS NULL OR
+      ts_rank(t.search_vector, query) < $2::numeric OR
+      (ts_rank(t.search_vector, query) = $2::numeric AND (
+        t."createdAt" < $3::timestamptz OR
+        (t."createdAt" = $3::timestamptz AND t.id < $4::text)
+      ))
+    )
+  ORDER BY rank DESC, t."createdAt" DESC, t.id DESC
+  LIMIT $5;
+  ```
- User search (parameterized — NEVER interpolate user input):
  ```sql
  SELECT id, username, "displayName", "avatarUrl", bio, "followerCount"
  FROM "User"
-  WHERE username ILIKE '%' || $1 || '%'
-    OR "displayName" ILIKE '%' || $1 || '%'
-  ORDER BY "followerCount" DESC
-  LIMIT 21
+  WHERE (
+        username ILIKE '%' || $1 || '%'
+        OR "displayName" ILIKE '%' || $1 || '%'
+      )
+    AND (
+      $2::int IS NULL OR
+      "followerCount" < $2::int OR
+      ("followerCount" = $2::int AND id < $3::text)
+    )
+  ORDER BY "followerCount" DESC, id DESC
+  LIMIT $4;
````

Use `prisma.$queryRaw` with `Prisma.sql` tagged template for safe parameterization. The `pg_trgm` GIN indexes (see §3 post-migration SQL) ensure this does not full-table-scan.
@@
-- User search returns by substring match ordered by popularity
+- User search returns by substring match ordered by popularity with stable cursor pagination
+- Search pagination remains stable across rank/follower-count ties
@@

### Phase E: Real-Time SSE (Tasks 10.1-10.6)

@@
-- Event format: `event: {type}\ndata: {json}\n\n`
+- Event format: `id: {seq}\nevent: {type}\ndata: {json}\n\n`

- Event types: `new-tweet` (payload: `{ tweetId, authorUsername }`), `notification` (payload: `{ notification }`)
  -- Publisher: uses Redis Pub/Sub. When a tweet is created, find all follower IDs, publish `new-tweet` to each. When a notification is created, publish `notification` to recipient. Store events in `sse:replay:{userId}` list for `Last-Event-ID` recovery.
  +- Publisher: uses Redis Pub/Sub. When a tweet is created, find all follower IDs, publish `new-tweet` to each. When a notification is created, publish `notification` to recipient.
  +- Sequence numbers: obtain per-user monotonic IDs via Redis `INCR sse:seq:{userId}`; include the resulting integer in the SSE `id:` field.
  +- Replay buffer: store serialized events in `sse:replay:{userId}` via `LPUSH`, `LTRIM 0 199`, and `EXPIRE 300` so `Last-Event-ID` reconnects can recover recent events without unbounded memory growth.
  @@
- New notification triggers `notification` event on client
- Connection auto-reconnects after drop
- Reconnect with `Last-Event-ID` does not lose recent events
  +- Replay buffer is bounded (`max 200` events per user key)
- Heartbeat keeps connection alive
  @@

#### H1: Unit Tests

@@
-| `tests/unit/media-url-validation.test.ts` | S3 URL origin validation, path prefix enforcement |
+| `tests/unit/media-upload-policy.test.ts` | Upload intent validation, MIME/size rules, origin/path enforcement |
+| `tests/unit/security-headers.test.ts` | CSP nonce generation and public-vs-self user select expectations |
@@

#### H2: Integration Tests

@@
-| `tests/integration/media.test.ts` | Pre-signed URL generation, URL validation on tweet create |
+| `tests/integration/media.test.ts` | Upload intent creation, confirmUpload verification, attach-on-tweet-create |
+| `tests/integration/csrf.test.ts` | Origin/host enforcement for unsafe cookie-authenticated methods |
+| `tests/integration/health.test.ts` | Live vs ready endpoint behavior under degraded Redis/S3 |
@@

#### H3: E2E Tests

@@
| `tests/e2e/specs/notification.spec.ts` | Notification list, bell badge, mark-read |
+| `tests/e2e/specs/security.spec.ts` | Cross-site mutation rejection and nonce-bearing CSP presence |
@@
-- Integration tests cover all critical flows
+- Integration tests cover all critical flows, including CSRF rejection, upload confirmation, and degraded health behavior
@@

### Risk 2: Feed Query Performance

Fan-out-on-read means the home timeline query joins Follow + Tweet for every request. For users following 1000+ people, this is slow. -**Mitigation:** Redis caching with 60s TTL. Cache invalidation on new tweet/follow. For v1 with limited scale, this is acceptable. The architecture supports migration to fan-out-on-write (Redis sorted sets per user) later. +**Mitigation:** Redis caching with 60s TTL, cache versioning on follow/unfollow/retweet/delete, and SSE-triggered client refresh on new tweets. For v1 with limited scale, this is acceptable. The architecture supports migration to fan-out-on-write (Redis sorted sets per user) later.
@@

### Risk 6: Image Upload Reliability

-Direct client-to-S3 upload can fail silently, leaving orphaned URLs in tweets. -**Mitigation:** The tweet creation mutation verifies that all mediaUrls match the expected S3 bucket origin and path prefix `tweet/{userId}/`. Reject any URL pointing to external domains. Pre-signed URLs expire in 10 minutes, so stale URLs cannot be reused.
+Direct client-to-S3 upload can fail silently, leaving orphaned objects or incomplete uploads. +**Mitigation:** Use `MediaUpload` intent records, require `confirmUpload` before attachment, resolve only owned `mediaUploadIds` during tweet/profile mutations, and run `scripts/reap-expired-media.ts` to delete expired/unattached objects.
@@

### Risk 9: SSE Connection Leak Under Load

Each SSE connection holds a Redis Pub/Sub subscriber. If connections are not properly cleaned up (browser closes without sending close event), subscribers accumulate. -**Mitigation:** Implement heartbeat ping every 30 seconds. If the write fails (broken pipe), unsubscribe and clean up immediately. Cap concurrent connections per user (max 5 per userId) and log warnings when the global connection count approaches configured limits. +**Mitigation:** Implement heartbeat ping every 30 seconds. If the write fails (broken pipe), unsubscribe and clean up immediately. Cap concurrent connections per user (max 5 per userId) and log warnings when the global connection count approaches configured limits. Bound replay storage with `LTRIM` and a max per-user buffer.

- +### Risk 10: Cross-Site Mutation Abuse
  +Cookie-authenticated mutation endpoints can be targeted from other origins if origin/host validation is omitted or inconsistently applied. +**Mitigation:** Enforce allow-listed `Origin` and `Host` checks for all unsafe methods in `src/middleware.ts`, test them in integration + E2E, and log blocked attempts with `requestId`.
- +### Risk 11: Orphaned / Replayed Media Uploads
  +Presigned uploads can leave behind unattached objects, and raw URL acceptance would allow stale object reuse. +**Mitigation:** Use `MediaUpload` intent records plus `confirmUpload`, attach only owned confirmed uploads, and run `scripts/reap-expired-media.ts` to expire and delete orphaned objects.

```

```
